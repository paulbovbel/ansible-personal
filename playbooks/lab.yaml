---
- hosts: app_server
  roles:
    - base
    - cockpit
    - caddy

- hosts: media_server
  roles:
    - base
    - cockpit
    - caddy
    - media-server

- hosts:
    - app_server
    - media_server
  tasks:
    - name: update all remaining packages
      apt:
        upgrade: dist
        force: true
      become: true
    - name: clean apt
      shell: apt-get autoremove --purge -y && apt-get clean
      become: true
